{
  "name": "local-code-brain-cli",
  "version": "0.1.0",
  "description": "Local Code Brain CLI - local-first repo intelligence for AI coding agents.",
  "bin": {
    "brain": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "dev": "ts-node src/index.ts",
    "test": "npm run build && node --test \"test/*.test.js\""
  },
  "dependencies": {
    "@local-code-brain/shared": "0.1.0",
    "axios": "^1.6.0",
    "chalk": "^4.1.2",
    "commander": "^12.0.0",
    "simple-git": "^3.24.0",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}
